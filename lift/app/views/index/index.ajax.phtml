<? foreach ($posts as $item): ?>
    <li class="entry-list-item" data-entry-list-max-amount="<?= $pagination['amount'] ?>">
        <? $this->view->partial('partials/entry', [
            'item' => $item,
            'type' => 'post'
        ]) ?>
    </li>
<? endforeach ?>

